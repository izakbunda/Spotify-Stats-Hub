// import { createSlice } from "@reduxjs/toolkit";

// const initialState = {
//   accessToken: "null", // your initial questions
// };

// const tokenSlice = createSlice({
//   name: "token",
//   initialState, // this is equal to initialState: initialState,
//   reducers: {
//     // reducers are functions to mutate the state
//     resetState: () => initialState,
//     setAccessToken: (state, action) => {
//       state.accessToken = action.payload;
//     },
//   },
// });

// export const _token = tokenSlice.actions; //_quiz will be the object that stores all your reducers
// export const $token = ({ token }) => token; //$quiz will be the object that stores all your variables
// export default tokenSlice.reducer;
